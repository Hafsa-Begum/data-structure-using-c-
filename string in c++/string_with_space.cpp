#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a;
    cin>>a;
    // cin.ignore(); //to ignore enter after entering integer
    getchar(); //to ignore enter after entering integer. this is used in c/c++
    string s;
    //cin.getline(s, 100); char s[100];
    getline(cin, s);
    cout<<s<<endl;
    return 0;
}
